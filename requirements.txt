# Runtime dependencies. GTK and PortAudio development libraries are installed
# by setup.sh on Debian/Ubuntu before pip resolves the native Python packages.
PyAudio>=0.2.13,<1
webrtcvad>=2.0.10,<3
PyGObject>=3.42,<4
python-dotenv>=1.0,<2
